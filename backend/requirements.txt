fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.13.4
pydantic-settings==2.7.0
psycopg2-binary==2.9.10
boto3==1.35.90
pytrends==4.9.2
python-dotenv==1.0.1
APScheduler==3.10.4
mcp==2.0.0
httpx2==2.10.0
mangum==0.21.0
# mcp==2.0.0 pulls a newer starlette by default; pinned back explicitly so
# it stays compatible with fastapi==0.115.6's starlette<0.42 requirement.
# (mcp's SSE *server* transport wants starlette>=0.49, but Humin only uses
# mcp as an MCP *client* - streamable-http - which doesn't need it.)
starlette==0.41.3
